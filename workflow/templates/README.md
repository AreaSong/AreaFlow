# Workflow Templates

本目录后续保存 AreaFlow 通用 workflow template 和 profile template。Phase 0 只记录目录职责，不提供可执行模板。

模板必须区分：

```text
profile template
stage template
artifact template
approval template
execution template
closeout template
```
