# AreaFlow Schemas

This directory contains stable machine-readable contracts shared by CLI, API,
Web, Desktop and managed project compatibility surfaces.

Current schemas:

- `status-projection.schema.json`: stable fallback `.areaflow/status.json`
  projection used by managed project shims and offline status readers.
