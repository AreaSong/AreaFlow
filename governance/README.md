# Governance

AreaFlow governance 记录平台安全、权限、workflow、adapter 和外部能力边界。它约束 AreaFlow 自己的行为，也约束 AreaFlow 如何接入被管理项目。

## 子域

- `security/`: 安全和密钥边界。
- `permissions/`: capability、路径、命令和 deny 优先策略。
- `workflow/`: stage engine、profile 和 cutover 规则。
- `adapters/`: project adapter 接入规则。
