# Overview 页面

Overview 是平台总览，不承载所有详情和管理操作。

当前展示：

- workflow version 和 artifact 数量。
- worker pool 中的 queued task 与 online worker 数量。
- 项目 readiness 与 operations 状态。
- 主要项目健康检查。
- 最近 domain events。

每个摘要都应链接到对应资源页面；详细 workflow、run、worker、artifact 和 audit 信息不在 Overview 重复展开。
